#Function Arguments: The values we pass to a function when calling it, so the function can use them to perform its task.


def add(a, b):          # a and b = arguments (inputs)
    return a + b        # returning the result (output)

# Function call
result = add(5, 3)      # here 5 and 3 are arguments
print(result)           # Output: 8
